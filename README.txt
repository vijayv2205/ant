#Steps to install framework

1. Take clone from git 
2. Import test.sql in your phpmyadmin(mysql) 
3. If node_modules folder is not exist then run `npm install` on root.
4. Run application by `node app.js`
5. You can see example user routes in routes folder, example user controller are in controllers folder and example user model in models folder.